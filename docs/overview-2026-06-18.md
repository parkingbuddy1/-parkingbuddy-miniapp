# 2026-06-18 修复汇总

## 修复清单（6项全部完成）

### ✅ 1. 订单页团购价计算 Bug
**问题**：详情页显示团购价（如833/807.5），订单页用现价×0.88/0.78重新计算（如748/663），完全不一致。
**修复**：
- `order.js calc()` → 优先使用数据源 `groupPrice3`/`groupPrice15` 字段
- `order.js onSubmitOrder()` → 拼团模式下传 `groupPrice` 而非原价作为 `unitPrice`
- `cloudfunctions/order/index.js` → 去掉硬编码 0.88/0.78 折扣，直接使用前端传来的实际价格

### ✅ 2. onGroupBuy15 传参 Bug
**问题**：`detail.js` 企业团按钮传 `groupType=10`，应为 `15`
**修复**：
- `detail.js:235` → `groupType=10` → `groupType=15`（并补充 `pkgIdx` 参数）
- `order.js calc()` → `groupType === '10'` → `groupType === '15'`
- `order.wxml:51` → `企业团(10人起)` → `企业团(15人起)`

### ✅ 3. Parking 云函数部署
**状态**：JSON 数据已含 100% 坐标（月卡1161/1161，次卡361/361），只需在微信开发者工具右键部署
**操作**：右键 `cloudfunctions/parking` → 「上传并部署：云端安装依赖」

### ✅ 4. 云环境 ID
**状态**：已在 `app.js` 配置 `cloudbase-d2gwr44k8e3e86a0f`，无需额外操作

### ✅ 5. Tab 图标
**产出**：10个 PNG 图标（81×81），暗黑风格
- `images/tab_home.png` / `tab_home_active.png`
- `images/tab_search.png` / `tab_search_active.png`
- `images/tab_ai.png` / `tab_ai_active.png`
- `images/tab_order.png` / `tab_order_active.png`
- `images/tab_mine.png` / `tab_mine_active.png`
- 颜色：默认 `#8E8E93`，选中 `#FF6B35`
- `app.json` 已配置 tabBar（5个标签页）
- 4处 `navigateTo`/`redirectTo` 已改为 `switchTab`

### ✅ 6. 微信支付商户号
**代码状态**：云函数支付逻辑完整就绪
**待办**：`docs/wechat-pay-guide.md` 提供完整开通指引

## 修改文件清单
| 文件 | 修改内容 |
|---|---|
| `miniprogram/pages/order/order.js` | calc()团购价、onSubmitOrder传价、switchTab |
| `miniprogram/pages/order/order.wxml` | 企业团文案(10→15人) |
| `miniprogram/pages/detail/detail.js` | onGroupBuy15传参+switchTab |
| `miniprogram/pages/index/index.js` | AI页switchTab |
| `cloudfunctions/order/index.js` | 去掉硬编码折扣 |
| `miniprogram/app.json` | 新增tabBar配置 |
| `miniprogram/images/` | 新增10个Tab图标 |
| `docs/wechat-pay-guide.md` | 支付开通指引 |

## 需手动操作
1. 微信开发者工具右键 `cloudfunctions/parking` → 上传并部署
2. 微信开发者工具右键 `cloudfunctions/order` → 上传并部署
3. 编译小程序测试 Tab 图标和导航
4. 按 `docs/wechat-pay-guide.md` 开通微信支付
